// FSD Layer: views
// Slice: tour-detail
// Tour detail page composition — route details, map, attractions
// Imports allowed: widgets, features, entities, shared

export { TourDetailPage } from './ui/TourDetailPage';
